import React from 'react';

const Component = () => {
    return (
        <div>
            <button>Click</button>
            <input type="text" placeholder="test"/>
        </div>
    );
};

export default Component;